# Architecture Decision Records

ADR记录会长期影响架构、范围或工作流的已确认决策。聊天建议、临时实验和未决想法不直接成为ADR。

命名：`NNNN-short-title.md`，例如 `0001-jj-as-version-control.md`。

每份ADR包含：状态、日期、背景、决策、理由、影响、替代方案和验证方式。使用 `0000-template.md` 创建新记录。
